# 安全与隐私说明

## 权限模型

- 程序清单默认使用 `asInvoker`，即继承当前用户权限；
- 管理员权限是可选项，仅用于提高部分硬件温度传感器的可见性；
- 提权通过 Windows 标准 `runas` 和 UAC 完成，程序不保存管理员凭据；
- 设置和开机启动项只写入 `HKEY_CURRENT_USER`。

## 数据与网络

程序读取以下本地数据：

- Windows CPU 时间计数器；
- Windows 网络适配器累计字节数；
- LibreHardwareMonitor 提供的 CPU 传感器；
- Windows ACPI 温区；
- 本程序自己的当前用户注册表设置。

程序不上传这些数据、不包含遥测，也不主动访问远程服务。构建时 NuGet 可能访问包源以还原依赖，这与运行时行为无关。

## 本地日志

异常日志位于 `%LOCALAPPDATA%\CpuNetWidget\Logs`，当前日志限制约 1 MiB，并保留一份轮换日志。日志可能包含异常堆栈和本地路径。分享日志前应检查并删除不希望公开的信息。

## 外部依赖

运行时第三方依赖为 `LibreHardwareMonitorLib 0.9.6`。依赖版本固定在项目文件中，许可证信息见 `THIRD-PARTY-NOTICES.txt`。发布前建议在可联网环境执行 NuGet 漏洞审计，并根据兼容性测试结果决定是否升级依赖。

LibreHardwareMonitor 可能使用底层硬件访问组件，这些组件可能被 Windows Defender 或 Windows 的易受攻击驱动程序阻止策略拦截。本项目不会关闭安全策略、修改阻止列表或自动安装 PawnIO。应优先接受温度不可用，而不是降低 Windows 安全保护。

2026-09-19 使用 NuGet 官方漏洞源对顶级包和全部传递依赖执行检查，结果未报告已知漏洞。该结果只代表检查时数据库中的公开信息。

## 发布建议

- 从干净的 Git 工作区构建；
- 使用可信的 .NET 8 SDK；
- 校验发布文件哈希；
- 面向其他用户分发时使用受信任的代码签名证书；
- 不要以管理员权限运行来源不明的构建产物。

## 报告问题

报告安全问题时请提供复现步骤、Windows 版本、是否使用管理员权限以及相关日志片段。请先移除用户名、本地路径或其他隐私信息。
