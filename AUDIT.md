# 代码审计记录

审计日期：2026-09-19

## 范围

本次审计覆盖：

- CPU、网络和温度数据读取；
- 曲线时间范围与数值格式；
- 设置、注册表和开机启动；
- 管理员权限请求；
- WPF 窗口、拖动、托盘、贴边与多显示器 DPI；
- 异步刷新、退出流程和资源释放；
- 构建脚本与温度诊断工具；
- README、安全和第三方许可证说明。

## 已修复项目

- CPU 系统计数器回退时可能发生无符号下溢；
- CPU 首次或无效样本被误显示为 0%；
- 网速间隔使用系统时间，系统校时可能造成错误速率；
- 网卡读取失败后恢复时可能出现速率峰值；
- 无效速率、读取失败和首次基线可能保留过期界面数值；
- 曲线按样本数近似时间，刷新延迟时范围不够准确；
- 重复创建状态画刷造成不必要的短期对象；
- 设置窗口可能被重复打开；
- 开机启动注册表和可执行文件路径缺少完整校验；
- 完整窗口切换模式时没有保留用户调整过的高度；
- 最小化或隐藏期间可能误触发贴边判断；
- 多显示器工作区坐标换算和窗口边界约束不够稳健；
- 退出时单个资源释放异常可能阻止其他资源释放；
- 温度读取、ACPI 和网络异常缺少受控、限频的诊断记录；
- 构建脚本生成图标时没有显式释放原生图标句柄；
- 构建脚本只检查“存在 SDK”，没有确认 .NET 8 SDK；
- 温度诊断工具异常退出时可能不关闭硬件监控器；
- README 缺少数据口径、隐私、日志、尺寸和排障说明。

## 依赖审计

- 直接依赖固定为 `LibreHardwareMonitorLib 0.9.6`；
- 官方 NuGet 页面与上游发布页显示 0.9.6 为当前稳定版本；
- 使用 NuGet 官方漏洞源执行 `--vulnerable --include-transitive`，顶级包及传递依赖均未报告已知漏洞；
- 已记录底层硬件访问可能被 Windows 安全策略阻止的风险，不建议用户关闭 Defender 或驱动阻止策略；
- 公开漏洞数据库并不能证明依赖绝对安全，后续发布仍需重新检查。

## 验证要求

- 主项目以 Release、`TreatWarningsAsErrors=true` 编译；
- 温度诊断项目以 Release、`TreatWarningsAsErrors=true` 编译；
- 使用 `Build.ps1` 重新生成自包含单文件 EXE；
- 启动 EXE 并确认主窗口可创建、监控刷新正常、进程可退出；
- 使用 `git diff --check` 检查补丁格式。

## 本次验证结果

- 主项目 Release 严格编译：0 个警告，0 个错误；
- 温度诊断项目 Release 严格编译：0 个警告，0 个错误；
- `dotnet format --verify-no-changes`：通过；
- NuGet 官方漏洞源（含传递依赖）：未报告已知漏洞；
- `Build.ps1 -SkipPackageAudit` 自包含单文件发布：通过；
- 最终 EXE 窗口创建、监控文本加载和应用内正常退出：通过；
- 完整面板切换迷你条：`63 × 285 px`（当前系统 125% 缩放）；
- 左侧贴边灰条：`9 × 63 px`；
- 点击灰条恢复：`63 × 285 px`；
- 贴边测试结束后通过应用内退出按钮正常结束进程。

## 剩余限制

静态审计和本机运行验证不能证明所有硬件、驱动和 Windows 版本上的行为完全一致。温度传感器可用性、多网卡/VPN 计数口径、UAC 策略和第三方库行为仍依赖目标电脑环境。当前仓库没有自动化 UI 测试和硬件矩阵测试，因此这些项目应在发布前进行人工兼容性测试。
